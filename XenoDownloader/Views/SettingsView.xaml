<UserControl x:Class="XenoDownloader.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="32" MaxWidth="850">
            <!-- Header -->
            <TextBlock Text="Settings &amp; Preferences" FontSize="26" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,6"/>
            <TextBlock Text="Configure storage paths, performance throttling, system tray behavior, and automation." FontSize="14" Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,0,24"/>

            <!-- General Settings Card -->
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="Download Directory" FontSize="15" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,4"/>
                    <TextBlock Text="Default folder where media streams and video files will be saved." FontSize="12" Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Style="{StaticResource ModernTextBoxStyle}" Text="{Binding CustomDownloadFolder}" Height="40" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding BrowseFolderCommand}" Height="40" Padding="16,0">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconFolder}" Fill="{StaticResource BrushTextPrimary}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                                <TextBlock Text="Browse Folder" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Separator Background="{StaticResource BrushBorder}" Margin="0,8,0,16"/>

                    <!-- 4K Video Downloader Smart Mode -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Smart Mode (1-Click Instant Download)" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushAccentCyan}"/>
                            <TextBlock Text="When enabled, analyzing or pasting video links starts downloading automatically using your preset resolution (e.g. 4K Ultra HD)." FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="{Binding SettingsService.CurrentSettings.SmartModeEnabled, Mode=TwoWay}" VerticalAlignment="Center"/>
                    </Grid>

                    <Separator Background="{StaticResource BrushBorder}" Margin="0,8,0,16"/>

                    <!-- Max Simultaneous Downloads -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Maximum Concurrent Downloads" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="Limits simultaneous network tasks to prevent network saturation and CPU spikes on low-end PCs." FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" Width="100" Height="36" VerticalContentAlignment="Center"
                                  SelectedValue="{Binding SettingsService.CurrentSettings.MaxConcurrentDownloads, Mode=TwoWay}"
                                  SelectedValuePath="Tag">
                            <ComboBoxItem Content="1" Tag="1"/>
                            <ComboBoxItem Content="2" Tag="2"/>
                            <ComboBoxItem Content="3 (Default)" Tag="3" IsSelected="True"/>
                            <ComboBoxItem Content="5" Tag="5"/>
                            <ComboBoxItem Content="8" Tag="8"/>
                        </ComboBox>
                    </Grid>

                    <Separator Background="{StaticResource BrushBorder}" Margin="0,8,0,16"/>

                    <!-- Clipboard Detection -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Automatic Clipboard URL Detection" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="Periodically detects video URLs copied to Windows clipboard without high CPU polling." FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="{Binding SettingsService.CurrentSettings.AutoClipboardDetect, Mode=TwoWay}" VerticalAlignment="Center"/>
                    </Grid>

                    <Separator Background="{StaticResource BrushBorder}" Margin="0,8,0,16"/>

                    <!-- Start with Windows -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Start With Windows" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="Launches Xeno Downloader minimized to tray upon user login." FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="{Binding SettingsService.CurrentSettings.StartWithWindows, Mode=TwoWay}" VerticalAlignment="Center"/>
                    </Grid>

                    <Separator Background="{StaticResource BrushBorder}" Margin="0,8,0,16"/>

                    <!-- Minimize to Tray -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Minimize to System Tray" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="Keep the application and background downloads running in the Windows taskbar notification area." FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="{Binding SettingsService.CurrentSettings.MinimizeToTray, Mode=TwoWay}" VerticalAlignment="Center"/>
                    </Grid>

                    <Separator Background="{StaticResource BrushBorder}" Margin="0,8,0,16"/>

                    <!-- Show Notifications -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Desktop Notifications" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="Display Windows notifications when a download completes or encounters an error." FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="{Binding SettingsService.CurrentSettings.ShowNotifications, Mode=TwoWay}" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Actions & Maintenance -->
            <Border Style="{StaticResource CardBorderStyle}" Padding="20" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding OpenLogsCommand}" Margin="0,0,12,0">
                            <TextBlock Text="View Diagnostic Logs"/>
                        </Button>
                        <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ResetSettingsCommand}">
                            <TextBlock Text="Reset to Defaults"/>
                        </Button>
                    </StackPanel>
                    <Button Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveSettingsCommand}" Height="40" Padding="24,0">
                        <TextBlock Text="Save Changes" FontWeight="Bold"/>
                    </Button>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
